defmodule Slipstream.TestingBallast do
  @moduledoc false

  # the testing ballast is a GenServer that simply sits and mediates between
  # Slipstream clients and fake servers spawned it test processes
end
